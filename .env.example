# --- REQUIRED: set these before launching ---

# Public site details
NEXT_PUBLIC_SITE_NAME=SavvyDealsHub
NEXT_PUBLIC_SITE_URL=http://localhost:8000

# Amazon Associates
AMAZON_ASSOC_TAG=savvydeals03d-21
AMAZON_ACCESS_KEY=
AMAZON_SECRET_KEY=
AMAZON_PARTNER_API_BASE=https://api.example.com/affiliate   # replace with your provider

# Google AdSense (client id is public, the rest server-side if needed)
NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-xxxxxxxxxxxxxxxx

# Google Analytics 4 (measurement ID is public)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# PayPal
PAYPAL_WEBHOOK_ID=
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_ENV=sandbox

# Email for daily reports
REPORTS_FROM="SavvyDealsHub Reports <no-reply@savvydealshub.com>"
REPORTS_TO=you@example.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# JSON Web Token secret (generate a strong random string)
JWT_SECRET=change-me-to-a-long-random-string

# Database (SQLite default)
DATABASE_URL="file:./prisma/dev.db"

# Secure ingestion API key (used for /api/ingest)
INGEST_API_KEY=

# Optional: wrap outbound product links with a template (affiliate tracking).
# Paste your eBay Partner Network "rover" template once approved.
# Must contain {{url}} (URL-encoded) OR {{urlRaw}}.
# Example:
# NEXT_PUBLIC_AFFILIATE_LINK_TEMPLATE=https://rover.ebay.com/rover/1/...?...&mpre={{url}}
NEXT_PUBLIC_AFFILIATE_LINK_TEMPLATE=

# Hourly refresh endpoint (external scheduler can call /api/cron/refresh)
CRON_SECRET=

# Optional: pull new deals from your own hosted JSON feed
# (e.g. a GitHub raw file). If empty, the app refreshes using the local sample feed.
DEALS_FEED_URL=
